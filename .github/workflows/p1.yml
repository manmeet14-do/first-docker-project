name: practice input


on:
 workflow_dispatch:
  inputs:
    environment:
     description: "select Environment"
     default: "dev"
     required: true
     type: choice
     options:
         - dev
         - prod
         - staging
  

jobs:
  demo:
   runs-on: ubuntu_latest
   


   steps:
     - name: Print hello
       run: echo "Hello"
